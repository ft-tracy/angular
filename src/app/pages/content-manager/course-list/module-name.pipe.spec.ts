import { ModuleNamePipe } from './module-name.pipe';

describe('ModuleNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ModuleNamePipe();
    expect(pipe).toBeTruthy();
  });
});
